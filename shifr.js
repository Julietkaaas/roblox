//async function hashData(data) {
    // Преобразуем данные в формат Uint8Array
    //const encoder = new TextEncoder();
    //const dataUint8 = encoder.encode(data);

    // Хешируем данные с использованием алгоритма SHA-256
    //const hashBuffer = await crypto.subtle.digest('SHA-256', dataUint8);

    // Преобразуем ArrayBuffer в строку шестнадцатеричных значений
   // const hashArray = Array.from(new Uint8Array(hashBuffer));
    //const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');

   // return hashHex;
//}

// Пример использования
//hashData("Это данные для хеширования").then(hash => {
  //  console.log("Хеш:", hash);
//});
